# Dependency & Supply Chain Security (React)

## Checks
1. Lockfile + frozen CI installs.
2. Audit React/Next and UI kit versions for known advisories.
3. Avoid unmaintained component libraries on critical paths.

## Important
- Duplicate React copies in bundle (peer dependency mistakes).
