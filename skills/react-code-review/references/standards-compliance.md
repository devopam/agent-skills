# Standards Compliance (React)

## Checks
1. React 18+ patterns; Concurrent-ready where claimed.
2. ESLint `react-hooks` rules enforced; `jsx-a11y` recommended (web+).
3. TypeScript preferred for apps beyond script tier.
4. Framework conventions (Next.js app router boundaries, etc.) followed consistently.

## Important
- Disabled `react-hooks/exhaustive-deps` globally without rationale.
