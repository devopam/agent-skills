# Normalize line endings for text files.
* text=auto eol=lf

# No large-binary assets expected in this repo (docs-only skills). If a
# future skill needs to bundle images, PDFs, or other large binaries, add
# Git LFS rules here rather than committing them directly, e.g.:
#   *.png filter=lfs diff=lfs merge=lfs -text
