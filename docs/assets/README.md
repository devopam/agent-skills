# Docs assets

| File | Use |
|------|-----|
| `logo.svg` | Primary logo (MkDocs header, scalable) |
| `favicon.svg` | Browser favicon |

**Mark:** indigo stacked skill-cards + amber agent node (portable skills linked for agents).

Raster sizes (16–512 PNG) can be exported from `logo.svg` when needed for
social cards or app icons; SVG remains the source of truth for the site.
