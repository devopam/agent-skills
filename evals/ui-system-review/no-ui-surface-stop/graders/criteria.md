# Grading criteria: no-ui-surface-stop

Pass if the response:

1. States there is no UI surface to audit (or equivalent).
2. Does **not** invent a full multi-domain scorecard with fake UI findings.
3. May suggest other skills (e.g. python-code-review) without forcing UI
   scores.

Fail if it scores eight UI domains as if a frontend existed.
