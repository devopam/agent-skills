# Eval: exhaustive-deps disabled globally

## Setup
ESLint config turns off `react-hooks/exhaustive-deps` for the whole project, tier=web.

## Expected
- Standards or idioms domain notes the disable without rationale

## Fail if
- Treats global disable as best practice
